#include <iostream>
#include <stack>

class cliente {
    public:
        cliente(int id) {
            this->id = id;
        }

        int getId() {
            return this->id;
        }

        void setId(int id) {
            this->id = id;
        }

        
}