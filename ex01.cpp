#include <iostream>
#include <math.h>

void soma(int num1, int num2)
{
    int soma = num1 + num2;
}

int main()
{
    int num1, num2;
    std::cin >> num1;

    std::cin >> num2;

    int soma = num1 + num2;

    /*||===||===||===||===| \/ |===||===||===||===||
    ||Positivo Capitão  | || | BROXA!!!!!!!!!   ||
    ||===||===||===||===| /\ |===||===||===||===||
  */
    std::cout << soma;

    return 0;
}