#include <stdio.h>
#include <stdlib.h>
#include "lib.h"

int main()
{
  float* projetos = (float*)malloc(10 * sizeof(float));
  Boleto boletos_recentes = (Boleto *)malloc(10* sizeof(Boleto));




  return EXIT_SUCCESS;
}
